## 1. Remove deprecated file

- [x] 1.1 Delete `middleware.ts` from the project root

## 2. Verify

- [x] 2.1 Confirm `pnpm dev` starts without the "Both middleware file and proxy file are detected" error
- [x] 2.2 Confirm protected routes still redirect unauthenticated users to `/login`
